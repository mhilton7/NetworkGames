# Uninstall

Detach USB, disconnect all NBD sessions, stop the app, and remove only the
Custom App. Do not select any option that deletes host-path datasets. Confirm
the source dataset file count and fingerprint manifest are unchanged.
Configuration, data, logs, backups, and certificates remain until an
administrator separately applies the retention policy.
