# NetworkGames LiveBridge

A Raspberry Pi network-backed USB mass-storage appliance.

The appliance presents an immutable raw disk image stored on an SMB server to a
Nintendo Wii through Linux USB gadget mode. USB Loader GX is the primary
Wii-side consumer.

Game data and private test images must never be committed to this repository.
